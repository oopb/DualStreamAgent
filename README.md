# DualStreamAgent

A minimal, local-first framework for an **evolvable, real-time streaming, dual-system multimodal agent** that can decide when to stay silent, respond immediately, or invoke slower reasoning.

The repository is being initialized with a causal runtime, local model backends, memory, skills, an evolution pipeline, and a scenario harness.